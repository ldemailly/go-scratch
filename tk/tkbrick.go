// Experiment with the Tk(9) package - see if I can replicate
// https://github.com/fortio/terminal#brick

package main

/*
import (
	"grol.io/grol/extensions"
	"grol.io/grol/object"
	grol "grol.io/grol/repl"

	"fortio.org/cli"
	"fortio.org/log"
	tk "modernc.org/tk9.0"
)

func main() {
	cli.Main()
	log.Infof("Hello, world!")
	grol.EvalString("Hello, world!")
	tkFunc := object.Extension{
		Name:     "pack",
		MinArgs:  1,
		MaxArgs:  -1,
		ArgTypes: []object.Type{object.STRING},
		//		Callback: func(args []object.Object) object.Object {

	}
	extensions.MustCreate(tkFunc)
	tk.Pack(tk.Button(tk.Txt("Hello"), tk.Command(func() { tk.Destroy(tk.App) })))
	tk.App.Wait()
}
*/
